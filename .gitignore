node_modules
.next
out
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
_legacy_angular

# Local pre-merge review artifacts (screenshots + PDFs)
resume-review/

# Isolated analysis build output (NEXT_DIST_DIR)
.next-analyze/
